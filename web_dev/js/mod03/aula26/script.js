/**
 * Escreva uma função chamada ehPaisagem que recebe dois argumentos:
 * largura e altura de uma imagem (number) e
 * retorne true se a imagem estiver no modo paisagem.
 */

const ehPaisagem = (largura, altura) => largura > altura;

console.log(ehPaisagem(297, 210));